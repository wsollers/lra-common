# Retired Bibliography Mirror

Bibliography is no longer owned by `lra-common`.

Each `lra-volume-*` repository owns the bibliography shard needed for its
standalone volume and book builds. There is no monorepo bibliography sync and
no common bibliography aggregation.

This directory remains only as historical migration context until the old
centralized bibliography is removed. Do not add canonical entries here.
